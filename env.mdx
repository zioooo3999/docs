---
title: "Environment Variables"
sidebarTitle: "Environment Variables"
description: "Reference guide for backend and frontend environment variables, database settings, and service connections."
---

## Overview

The Tempe Park platform utilizes environment files to manage system settings, security keys, database parameters, object storage, and service integration across the backend (Strapi) and frontend (Next.js) applications.

---

## Backend Environment Variables

The backend configuration is managed via `.env` located at the root of `web-be/`.

```bash web-be/.env
# Application Settings & Security Keys
HOST=0.0.0.0
PORT=1337
APP_KEYS=[key1],[key2]
API_TOKEN_SALT=[salt]
ADMIN_JWT_SECRET=[secret]
TRANSFER_TOKEN_SALT=[salt]
JWT_SECRET=[secret]
ENCRYPTION_KEY=[key]

# Database Credentials (PostgreSQL)
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=tempepark
DATABASE_USERNAME=[user]
DATABASE_PASSWORD=[password]
DATABASE_SSL=false
DATABASE_SCHEMA=public

# Object Storage (MinIO / S3-compatible)
MINIO_BASE_URL=https://[host]/tempe-park
MINIO_ENDPOINT=https://[host]
MINIO_ACCESS_KEY=[access-key]
MINIO_SECRET_KEY=[secret-key]
MINIO_REGION=us-east-1
MINIO_BUCKET=tempe-park
```